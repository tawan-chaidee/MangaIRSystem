{
  "name": "elasticsearch",
  "version": "1.0.0",
  "description": "",
  "main": "test.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tool:scraper": "cd tools && npm run scraper",
    "tool:db": "cd tools && npm run db",
    "server": "cd backend && npm start",
    "client": "cd frontend && npm run dev"
  },
  "workspaces": ["frontend","backend","tools"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@elastic/elasticsearch": "^8.10.0",
    "typescript": "^5.3.2",
    "dotenv": "^16.3.1"
  }
}
